# accepted-behaviors.yaml — a risk-acceptance register.
#
# Every entry is a behavior treated as ACCEPTED: as long as an entry exists, the
# tool considers that behavior intended and does not re-report it. To stop
# accepting one, delete it — the finding comes back. Version control records who
# changed what, and when.
#
#   consequence:  [tool] the precise risk, in plain terms a non-expert can
#                 evaluate — what could actually happen. (max 1000 runes)
#   evidence:     [tool] proof it looks intentional — comment / test / flag /
#                 code, by file:line.
#   reason:       [human] the team's rationale; the tool never writes it.
#
# This file is APPEND-ONLY. Keep every existing entry and only append.

accepted_behaviors:
  - consequence: "An organization administrator can configure the LDAP connector to connect from the Casdoor server to loopback or other internal hosts, exposing connection success, failure, and timing as an internal-network oracle."
    evidence: "web/src/LdapEditPage.js:144 exposes editable LDAP server host and port controls, and object/ldap_conn.go:103 deliberately dials that configured endpoint."
  - consequence: "Anyone who knows a pending callback URL for an order using the Dummy payment provider can mark that payment and order paid without authenticating or presenting an external-provider signature."
    evidence: "pp/dummy.go:35-38 identifies Dummy as a test/mock provider whose order data is kept internally for testing, and pp/dummy.go:55-70 deliberately accepts that internal data and returns a Paid result."
