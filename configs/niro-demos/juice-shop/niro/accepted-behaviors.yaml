# accepted-behaviors.yaml — a risk-acceptance register.
#
# Every entry is a behavior treated as ACCEPTED: as long as an entry exists, the
# tool considers that behavior intended and does not re-report it. To stop
# accepting one, delete it — the finding comes back. Version control records who
# changed what, and when.
#
#   consequence:  [tool] the precise risk, in plain terms a non-expert can
#                 evaluate — what could actually happen. (max 1000 runes)
#   evidence:     [tool] proof it looks intentional — comment / test / flag /
#                 code, by file:line.
#   reason:       [human] your team's rationale for accepting this — your own
#                 documentation. The tool never writes it, and an entry is
#                 accepted by being here, not by this field. But fill it in: an
#                 undocumented acceptance no one can explain later is how real
#                 risk quietly gets forgotten.
#
# Be specific enough to adjudicate in seconds. Vague entries hide real bugs.
#
# This file accumulates across runs and is APPEND-ONLY. Anyone updating it — tool
# or human — must read it first, keep every existing entry (especially the human
# `reason`), and only append; never rewrite, reorder, or drop entries. Presence
# is the suppression signal, so a clobbered entry silently un-accepts a finding.
# COMMIT it to git — that is your history, your review trail, and your undo.
#
# Create `accepted-behaviors.yaml` next to this file, carrying this header into
# it so its rules travel with the data.

accepted_behaviors:
  # TC-EB29B4DE — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any website can embed /rest/user/whoami as a <script> tag (JSONP via the `callback` query parameter) and, via a `fields` parameter, read the logged-in victim's email and MD5 password hash cross-domain, since the victim's session cookie is sent automatically with the script-tag request."
    evidence: "routes/currentUser.ts:54-59 explicitly implements the JSONP branch and calls challengeUtils.solveIf(challenges.emailLeakChallenge, ...) when it fires; data/static/challenges.yml:321-331 defines 'Email Leak' (key: emailLeakChallenge, category 'Sensitive Data Exposure', description 'Perform an unwanted information disclosure by accessing data cross-domain', hint 'This challenge uses an old way which is no longer recommended', mitigationUrl to the XS-Leaks cheat sheet). The password-hash-in-response half is likewise a named, scored challenge: data/static/challenges.yml:1-12 defines 'Password Hash Leak' (key: passwordHashLeakChallenge, 'Obtain the password (hash) of the currently logged-in user directly from a REST API endpoint'), solved via routes/currentUser.ts:52. OWASP Juice Shop is an intentionally-vulnerable training/CTF application, and this specific JSONP/data-exposure combination is one of its named, first-class, scored challenges (not a generic 'demo app' label) — the app is deliberately shipping this exact exploitable behavior for users to find and solve."
    reason: ""

  # TC-B3336E95 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any unauthenticated attacker can bypass the 100-request/5-minute rate limit on POST /rest/user/reset-password by rotating a spoofed X-Forwarded-For header value on every 100 attempts, giving unlimited guesses at a user's security-question answer and enabling account takeover via password reset."
    evidence: "server.ts:340-348 — the rate-limit keyGenerator that trusts the raw `X-Forwarded-For` header (`headers['X-Forwarded-For'] ?? ip`) is wrapped in `vuln-code-snippet start/end resetPasswordMortyChallenge` markers, with the offending line explicitly tagged `vuln-line resetPasswordMortyChallenge`; data/static/challenges.yml:976-989 defines this exact behavior as the 'Reset Morty's Password' challenge (category 'Broken Anti Automation', tags OSINT/Brute Force), whose own hint text reads 'Forgot your password? is protected by a rate limiting mechanism that prevents brute forcing. You need to beat this somehow.' Bypassing this specific rate limit is the documented, scored objective of a named, first-class challenge — not an accidental oversight."
    reason: ""

  # TC-3EED28F7 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any authenticated customer can add a basket item with a negative quantity (POST /api/BasketItems or PUT /api/BasketItems/:id) and check out to produce an order with a negative total price, which the platform records as a legitimate completed order; if paid via wallet, WalletModel.decrement is called with that negative total, which increments the customer's wallet balance instead of debiting it — effectively free store credit."
    evidence: "routes/order.ts:144 explicitly detects and marks this exact condition solved: `challengeUtils.solveIf(challenges.negativeOrderChallenge, () => { return totalPrice < 0 })`; data/static/challenges.yml:838-842 defines the named, scored challenge 'Payback Time' (key: negativeOrderChallenge, category 'Improper Input Validation', description 'Place an order that makes you rich.', hint 'You literally need to make the shop owe you any amount of money. Investigate the shopping basket closely to understand how it prevents you from creating orders that would fulfil the challenge.'). The missing lower-bound guard in routes/basketItems.ts's quantityCheck (~lines 85-100), which only checks stock/limitPerUser upper bounds and never rejects quantity < 1, is precisely the gap this first-class challenge exists for players to discover and exploit."
    reason: ""

  # TC-83CBE6E9 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any attacker who holds a victim's Bearer session token can permanently change the victim's password via GET /rest/user/change-password without ever supplying the victim's current password (omitting the `current` query parameter bypasses the check entirely), locking the victim out of their own account."
    evidence: "routes/changePassword.ts:39 — `if (currentPassword && security.hash(currentPassword) !== loggedInUser.data.password)` only checks the current password when one is supplied, so a missing `current` param short-circuits the guard; line 52-55 immediately calls `challengeUtils.solveIf(challenges.changePasswordBenderChallenge, () => user.id === 3 && !currentPassword && user.password === security.hash('slurmCl4ssic'))`, i.e. the app's own challenge-solved detection explicitly checks for `!currentPassword` as the winning condition. data/static/challenges.yml:196-205 defines this exact behavior as the named, scored challenge 'Change Bender's Password' (category 'Broken Authentication', description \"Change Bender's password into slurmCl4ssic without using SQL Injection or Forgot Password\", with hints and a mitigationUrl). test/cypress/e2e/changePassword.spec.ts:21-35 (describe block `challenge \"changePasswordBenderChallenge\"`) drives this precise flow — logging in as bender, changing his password without passing `current` — and asserts the challenge is solved (`cy.expectChallengeSolved`), i.e. the app's own test suite asserts the opposite of the finding's invariant. This is a named, first-class, scored CTF challenge deliberately shipped by the intentionally-vulnerable training app, not an accidental oversight."
    reason: ""

  # TC-8A965606 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any authenticated user can point the profile-image-by-URL feature (POST /profile/image/url) at an internal/loopback URL; the server fetches it with no IP restriction and writes the full response body to a publicly readable static file at /assets/public/images/uploads/{userId}.{ext}, letting the caller exfiltrate the content of internal-only endpoints (SSRF)."
    evidence: "routes/profileImageUrlUpload.ts:20 sets req.app.locals.abused_ssrf_bug = true when the supplied imageUrl matches /solve/challenges/server-side/; routes/verify.ts:102-103 calls challengeUtils.solve(challenges.ssrfChallenge) when that flag is true; server.ts:176 initializes the same flag. data/static/challenges.yml (key: ssrfChallenge) defines this exact endpoint/behavior as the named, scored 'SSRF' challenge (category 'Broken Access Control', description 'Request a hidden resource on server through server.', mitigationUrl to the OWASP SSRF Prevention Cheat Sheet). This is a deliberately built-in, first-class OWASP Juice Shop challenge — not an accidental oversight — with dedicated code tracking whether a player has solved it."
    reason: ""

  # TC-2BEBBAC0 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any website on the internet can use JavaScript fetch() to read the response of every API endpoint on this app, including the full server configuration at /rest/admin/application-configuration, and — if a victim's Bearer token is ever exposed to the attacking page — authenticated responses such as the full user list (id, email, role) at /api/Users."
    evidence: "server.ts:181-183 — the comment '/* Bludgeon solution for possible CORS problems: Allow everything! */' immediately precedes, and explicitly explains the intent behind, the unconfigured, global `app.options('*', cors())` / `app.use(cors())` calls. This documents the wildcard CORS policy as the deliberate 'solution' chosen by the developers, not an accidental default. Reproduced live: `curl -H 'Origin: https://evil.com' http://localhost:3000/rest/admin/application-configuration -I` returns `Access-Control-Allow-Origin: *`, and the OPTIONS preflight for the same origin with `Access-Control-Request-Headers: Authorization` returns `Access-Control-Allow-Headers: Authorization`, confirming the behavior described in the finding."
    reason: ""

  # TC-C678C2BA — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any visitor who opens a crafted order-tracking link (GET /rest/track-order/:id with an HTML/JS payload as :id, e.g. /#/track-result?id=<iframe src=\"javascript:alert(`xss`)\">) gets arbitrary JavaScript executed in their browser under the application's origin, exposing session cookies, auth tokens, and any in-app data to the attacker."
    evidence: "routes/trackOrder.ts:14-15 — comment 'Truncate id to avoid unintentional RCE' guards a deliberate branch: `const id = !utils.isChallengeEnabled(challenges.reflectedXssChallenge) ? String(req.params.id).replace(/[^\\w-]+/g, '') : utils.trunc(req.params.id, 60)` only sanitizes the id when the challenge is OFF, and trackOrder.ts:17 explicitly calls `challengeUtils.solveIf(challenges.reflectedXssChallenge, () => utils.contains(id, '<iframe src=\"javascript:alert(`xss`)\">'))` — the finding's exact payload is the literal win-condition check. data/static/challenges.yml:901-918 defines the named, scored challenge 'Reflected XSS' (key: reflectedXssChallenge, category 'XSS', description 'Perform a reflected XSS attack with <iframe src=\"javascript:alert(`xss`)\">', difficulty 2, tutorial order 5), matching the finding's payload verbatim. Frontend side, frontend/src/app/track-result/track-result.component.ts calls `this.sanitizer.bypassSecurityTrustHtml(...)` on the same field specifically to let the challenge payload render — an explicit bypass of Angular's default sanitizer, not an oversight. This is one of Juice Shop's original, first-class tutorial challenges, not a generic 'demo app' label — the app deliberately ships this exact exploitable behavior for users to find and solve. Note: the challenge is env-gated off in the app's own Docker image (data/static/challenges.yml:916-918 `disabledEnv: [Docker, Heroku]`, checked via lib/utils.ts:151-184 `getChallengeEnablementStatus`/`isChallengeEnabled`), but this harness starts the app via `node build/app.js` directly (niro/harness/start.sh), not the Docker image, so `isDocker()` is false and the challenge remains enabled — consistent with the finding's observed reproduction."
    reason: ""

  # TC-7FF95ADE — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any authenticated customer can add items to another customer's (including the admin's) shopping basket by sending a single POST /api/BasketItems with two BasketId fields — the ownership check reads the first (attacker-owned) value while the write uses the last (victim-owned) value, so the attacker's own basket ID passes the check while the victim's basket receives the item — polluting the victim's cart and disrupting their checkout until they notice and remove it."
    evidence: "routes/basketItems.ts:37-45 — addBasketItem() checks basketIds[0] for the ownership gate but builds the BasketItem from basketIds[basketIds.length - 1], and calls challengeUtils.solveIf(challenges.basketManipulateChallenge, () => user.bid != basketItem.BasketId) specifically when the written basket differs from the attacker's own basket. data/static/challenges.yml:707-716 defines this as a named, scored challenge ('Manipulate Basket', key basketManipulateChallenge, category 'Broken Access Control', description 'Put an additional product into another user's shopping basket', hints pointing directly at HTTP-traffic tampering). The app's own Cypress suite performs this exact duplicate-BasketId-key attack and asserts the challenge is solved: test/cypress/e2e/basket.spec.ts:54-68, POST body '{ \"ProductId\": 14,\"BasketId\":\"1\",\"quantity\":1,\"BasketId\":\"2\" }' followed by cy.expectChallengeSolved({ challenge: 'Manipulate Basket' }). OWASP Juice Shop is an intentionally-vulnerable training/CTF application, and this specific duplicate-JSON-key ownership bypass is one of its named, first-class, scored challenges (not a generic 'demo app' label) — the app deliberately ships this exact exploitable behavior for users to find and solve."
    reason: ""

  # TC-E753BDFC — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any authenticated user can set their profile username to a `#{...}` template payload (e.g. `#{global.process.mainModule.require('child_process').execSync('id').toString()}`) which the server evaluates with Node.js `eval()` when rendering GET /profile, executing arbitrary OS commands as the server process user and returning the output embedded in the profile page HTML — full remote code execution."
    evidence: "routes/userProfile.ts:54-67 — the eval branch (`username = eval(code)`) is explicitly gated by `utils.isChallengeEnabled(challenges.usernameXssChallenge)` and, when it fires, sets `req.app.locals.abused_ssti_bug = true` and calls `challengeUtils.solveIf(challenges.usernameXssChallenge, ...)` (line 90) to award the scored challenge. data/static/challenges.yml:219-234 defines this as the named, scored 'CSP Bypass' challenge (key: usernameXssChallenge, category 'XSS', tag 'Danger Zone', description 'Bypass the Content Security Policy and perform an XSS attack...', with hints and a mitigationUrl) and explicitly lists `disabledEnv: [Docker, Heroku]` — i.e. the maintainers deliberately keep this exploitable vulnerability live in default/bare-metal deployments (for CTF play) while consciously disabling it in Docker/Heroku deployments and whenever `challenges.safetyMode` is 'enabled' (lib/utils.ts:151-184). This is not a generic 'intentionally vulnerable app' label — it is a specific, named, capability-flag-gated challenge whose entire purpose is to be discovered and exploited exactly as this finding describes. Same pattern already accepted elsewhere (named JSONP/email-leak challenge)."
    reason: ""

  # TC-02CCB679 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any network-reachable attacker can submit unlimited password guesses against POST /rest/user/login (confirmed live: 15 consecutive wrong-password attempts against admin@juice-sh.op all returned 401 with no Retry-After/X-RateLimit headers and no slowdown, while a correct-password login on the same account still returns 200 normally), enabling unthrottled credential stuffing or dictionary/brute-force attacks against any account, most notably the admin account's weak password."
    evidence: "server.ts:596 — app.post('/rest/user/login', login()) has no rateLimit() middleware, unlike /rest/user/reset-password (server.ts:343) and /rest/2fa/verify (server.ts:459,466,472). This is the app's own documented, scored objective, not an oversight: data/static/challenges.yml:817-830 defines the 'Password Strength' challenge (key: weakPasswordChallenge, category 'Broken Authentication', tags Brute Force/Tutorial, description \"Log in with the administrator's user credentials without previously changing them or applying SQL Injection\", hint 'you can also go for a brute force attack using a generic password list' — i.e. exactly the admin login-guessing attack this finding demonstrates). data/static/challenges.yml:692-706 similarly defines 'Login Support Team' (tags Brute Force/Code Analysis) as guessing another account's login via repeated attempts. OWASP Juice Shop is a training/CTF app whose own challenge catalog names unthrottled login-guessing against specific accounts as an intended, scored solve path — not a generic 'intentionally vulnerable' label."
    reason: ""

  # TC-DEC9731E — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any anonymous visitor who registers via POST /api/Users can set \"role\":\"admin\" in the request body and immediately obtain a valid admin JWT on login, gaining full administrative access to all users' data, orders, products, and any admin-gated server functionality."
    evidence: "server.ts:420 wires `app.post('/api/Users', verify.registerAdminChallenge())` ahead of the Finale-generated create handler; routes/verify.ts:51-56 implements `registerAdminChallenge`, which calls `challengeUtils.solveIf(challenges.registerAdminChallenge, () => req.body && req.body.role === security.roles.admin)` — i.e. the app detects this exact mass-assignment request and marks it as a solved challenge rather than rejecting it. server.ts:479-518 brackets the whole Finale `/api/Users` auto-generated-endpoint block with `// vuln-code-snippet start/end registerAdminChallenge` markers, confirming the unfiltered role field is a deliberately preserved vulnerable code path. data/static/challenges.yml:42-51 defines the named, scored challenge 'Admin Registration' (key: registerAdminChallenge, category 'Improper Input Validation', description 'Register as a user with administrator privileges.', difficulty 3, with hints that walk the player through discovering and exploiting exactly this endpoint/field). This is not a generic 'intentionally vulnerable app' label — it is a specific, named, first-class CTF challenge whose win-condition is this precise mass-assignment exploit."
    reason: ""

  # TC-4B096581 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "An unauthenticated internet visitor can overwrite any product's description via PUT /api/Products/:id with raw, unsanitized HTML/script (e.g. <iframe src=\"javascript:alert(...)\"> or <img onerror=...>), which then executes in the browser of every user who subsequently opens that product's details dialog (stored XSS planted without any login)."
    evidence: "server.ts:370 — the `isAuthorized()` middleware for `PUT /api/Products/:id` is commented out, explicitly tagged `// vuln-code-snippet vuln-line changeProductChallenge`. models/product.ts:44-55 — the `description` setter skips `security.sanitizeSecure()` whenever `utils.isChallengeEnabled(challenges.restfulXssChallenge)` is true, instead calling `challengeUtils.solveIf(challenges.restfulXssChallenge, ...)` to check for exactly the `<iframe src=\"javascript:alert(`xss`)\">` payload — i.e. the sanitizer bypass exists specifically to let this payload succeed. Both are named, cataloged OWASP Juice Shop hacking challenges in data/static/challenges.yml: key `changeProductChallenge` ('Product Tampering', category 'Broken Access Control', description 'Change the description of a product ... via broken admin functionality or holes in the RESTful API') and key `restfulXssChallenge`. Reproduced live against the running target: unauthenticated `PUT http://localhost:3000/api/Products/1` with body `{\"description\":\"NIRO-VERIFY-MARKER-<img src=x onerror=alert(1)>\"}` returned HTTP 200 with the payload stored verbatim, unsanitized (confirmed via follow-up GET); product description was restored afterward to the state found."
    reason: ""

  # TC-5CE71744 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any authenticated user can retrieve their own MD5 password hash via GET /rest/user/whoami?fields=password. MD5 is trivially reversible via rainbow tables, so an attacker with a stolen session cookie can extract and crack the account password, gaining persistent credential access that survives session revocation and enables password-reuse attacks on other services."
    evidence: "routes/currentUser.ts:27-33 — the unrestricted field-copy loop that lets `?fields=` return arbitrary user-record keys including `password`; routes/currentUser.ts:52 explicitly calls `challengeUtils.solveIf(challenges.passwordHashLeakChallenge, () => response?.user?.password)` right after building the response, i.e. the app's own challenge-solved detection is wired to fire exactly when the password field leaks. data/static/challenges.yml:1-12 defines this exact behavior as the named, scored challenge 'Password Hash Leak' (category 'Sensitive Data Exposure', description 'Obtain the password (hash) of the currently logged-in user directly from a REST API endpoint', hints pointing at DevTools/proxy inspection and 'an overly generic solution for data retrieval can backfire if not properly safeguarded', mitigationUrl to the OWASP API Security excessive-data-exposure page). test/cypress/e2e/passwordHashLeak.spec.ts:1-19 (describe `challenge \"Password Hash Leak\"`) drives this precise flow — GET /rest/user/whoami?fields=id,email,password as admin — and asserts `res.body.user.password` is a non-empty string plus `cy.expectChallengeSolved({ challenge: 'Password Hash Leak' })`; test/api/user.test.ts:315-327 ('GET who-am-i with fields parameter can be tricked into returning password') likewise asserts `typeof res.body.user.password === 'string'`. The app's own test suite asserts the opposite of the finding's invariant — this is a named, first-class, scored CTF challenge deliberately shipped by the intentionally-vulnerable training app, not an accidental oversight."
    reason: ""

  # TC-A11A1709 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any unauthenticated visitor can log in as any account, including the admin, by sending a SQL-injection payload (e.g. email = \"' OR 1=1 --\") to POST /rest/user/login, bypassing password verification entirely and receiving a fully valid admin session token."
    evidence: "routes/login.ts:33-34 — the raw string-interpolated `models.sequelize.query(...)` call is wrapped in `// vuln-code-snippet start loginAdminChallenge loginBenderChallenge loginJimChallenge` / `// vuln-code-snippet end ...` markers, with the exact vulnerable line explicitly tagged `// vuln-code-snippet vuln-line loginAdminChallenge loginBenderChallenge loginJimChallenge`. data/static/challenges.yml:604-616 defines the named, scored challenge 'Login Admin' (key: loginAdminChallenge, category 'Injection', description \"Log in with the administrator's user account\", difficulty 2), whose hints explicitly point at this exact attack (\"a dedicated attack pattern\", i.e. SQL injection on the login form) and whose mitigationUrl links to the OWASP SQL Injection Prevention Cheat Sheet. data/static/codefixes/loginAdminChallenge_4_correct.ts additionally ships the parameterized-query fix as a teaching code-fix exercise for this same challenge. This is a named, first-class, scored CTF challenge deliberately shipped with a documented remediation path — not a generic 'intentionally vulnerable demo app' label."
    reason: ""

  # TC-3C0711D3 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any authenticated customer can retrieve any other customer's basket — its full product list, coupon codes, and UserId — by supplying an arbitrary basket ID in the URL (e.g. GET /rest/basket/3 with another user's JWT), giving visibility into another user's order intent and any discount codes attached to it."
    evidence: "routes/basket.ts retrieveBasket(): the handler fetches BasketModel by the raw req.params.id with no ownership comparison against the requesting user's `bid`; instead it calls challengeUtils.solveIf(challenges.basketAccessChallenge, () => user.bid != parseInt(id, 10)), i.e. successfully fetching someone else's basket is the literal win-condition check for a named challenge. data/static/challenges.yml:1197-1210 defines this as challenge 'View Basket' (key: basketAccessChallenge, category 'Broken Access Control', difficulty 2, description 'View another user''s shopping basket.', hints pointing solvers at manipulating the client-side basket id, mitigationUrl to the OWASP Authorization Cheat Sheet). The project's own Cypress e2e suite (test/cypress/e2e/basket.spec.ts, describe block 'challenge \"basketAccessChallenge\"') sets another user's basket id and asserts cy.expectChallengeSolved({ challenge: 'View Basket' }) — the app's own tests assert the opposite of the reported invariant, treating cross-user basket access as the expected, scored outcome rather than a bug. Live-reproduced against the running target: GET /rest/basket/3 with jim's (bid=2) JWT returned Bender's full basket (UserId=3) with HTTP 200."
    reason: ""

  # TC-8190CEF8 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any anonymous visitor can read the application's live Prometheus metrics at GET /metrics with no credential required — including total registered users, wallet balances aggregated across all accounts, order counts, and process internals (resident memory, CPU seconds) — giving an attacker a real-time business-intelligence feed and a fingerprint of the runtime for follow-on attacks."
    evidence: "server.ts:729 — `app.get('/metrics', utils.asyncHandler(metrics.serveMetrics()))` carries no auth middleware and is explicitly tagged `// vuln-code-snippet vuln-line exposedMetricsChallenge`; server.ts:728 tags the preceding `metrics.observeMetrics()` call `// vuln-code-snippet neutral-line exposedMetricsChallenge`. routes/metrics.ts:84-93 (`serveMetrics`) calls `challengeUtils.solveIf(challenges.exposedMetricsChallenge, ...)` on every hit to this exact unauthenticated route — i.e. the app's own challenge-solved detection fires precisely when an anonymous caller reaches this endpoint. data/static/challenges.yml:1336-1347 defines this exact behavior as the named, scored challenge 'Exposed Metrics' (category 'Observability Failures', tag 'Good Practice', description \"Find the endpoint that serves usage data to be scraped by a popular monitoring system\", hints pointing the player at guessing the default Prometheus scrape path). This is a named, first-class, scored CTF challenge deliberately shipped by the intentionally-vulnerable training app, not an accidental oversight."
    reason: ""

  # TC-B8ACF375 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any unauthenticated user can download files with blocked extensions (e.g., .bak, .gg, .yml) from the /ftp directory by appending a double-URL-encoded null byte followed by .md to the filename (e.g. GET /ftp/package.json.bak%2500.md), exposing backup files such as the dependency manifest, discount coupon codes, and a SIEM signature file that the extension allowlist is meant to block."
    evidence: "routes/fileServer.ts's verify() checks endsWithAllowlistedFileType(file) BEFORE calling security.cutOffPoisonNullByte(file), which is exactly what makes the encoded-null-byte bypass work; the very next function, verifySuccessfulPoisonNullByteExploit(file) (routes/fileServer.ts:39-47), explicitly detects and scores this exact exploit against file.toLowerCase() === 'eastere.gg', 'package.json.bak', 'coupons_2013.md.bak', and 'suspicious_errors.yml', and marks challenges.nullByteChallenge solved when any of them fire. data/static/challenges.yml:1425-1438 defines the named, scored challenge 'Poison Null Byte' (key: nullByteChallenge, category 'Improper Input Validation', difficulty 4) whose hints read: 'Some files in there are not directly accessible because a security mechanism prevents access,' 'The Poison Null Byte can trick the security mechanism into thinking that the file you want has a valid file type,' and 'Depending on the files you try to retrieve you will probably solve \"Access a developer's forgotten backup file\", \"Access a salesman's forgotten backup file\", \"Access a misplaced SIEM signature file\", or \"Find the hidden easter egg\" along the way' — describing this precise attack and its exact target files. Live-verified: GET /ftp/package.json.bak returns 403 while GET /ftp/package.json.bak%2500.md returns 200 on the running target. OWASP Juice Shop is an intentionally-vulnerable training/CTF application, and this specific null-byte bypass is one of its named, first-class, scored challenges (not a generic 'demo app' label) — the app deliberately ships this exact exploitable behavior for users to find and solve."
    reason: ""

  # TC-359A2F26 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any unauthenticated visitor can read the shared instance's current game-progress ('continue') code and re-apply it — or, since the Hashids salt is hardcoded and public, craft a brand-new code for arbitrary challenge IDs — via PUT /rest/continue-code/apply/:continueCode (and the findIt/fixIt variants) to mark any chosen set of challenges as globally solved, with no login, token, or session cookie required."
    evidence: "server.ts:612-614 registers all three apply routes (`/rest/continue-code-findIt/apply/:continueCode`, `/rest/continue-code-fixIt/apply/:continueCode`, `/rest/continue-code/apply/:continueCode`) with no `security.appendUserId()` or any auth middleware, unlike auth-gated endpoints such as `/rest/wallet/balance` (server.ts:627). routes/continueCode.ts:13,25,38 hardcodes the Hashids salts ('this is my salt', etc.) used to both encode and decode codes, so anyone who reads the public source can forge a code for any challenge IDs. This is not an oversight: data/static/challenges.yml:543-558 defines a named, scored challenge 'Imaginary Challenge' (key: continueCodeChallenge, category 'Cryptographic Issues', difficulty 6) whose description is literally 'Solve challenge #999. Unfortunately, this challenge does not exist.' and whose hints explicitly walk the player to this exact exploit: 'You need to trick the hacking progress persistence feature into thinking you solved challenge #999', 'Deduce ... how the application encrypts and decrypts your hacking progress', and 'What would be a really stupid mistake a developer might make when choosing such a secret?' — i.e. forging a continue code via the hardcoded salt to mark a nonexistent challenge solved is the documented objective. routes/restoreProgress.ts:24-26 contains the matching solve-detection: applying a code that decodes to id 999 explicitly solves `continueCodeChallenge`. Consistent with this, the continue-code feature is architecturally unauthenticated by design: frontend/src/app/server-started-notification/server-started-notification.component.ts:39-77 auto-applies a continue code from a plain (non-httpOnly-required) cookie on server start, before any login has occurred, and test/cypress/e2e/scoreBoard.spec.ts:10-27 (describe block `challenge \"continueCode\"`) drives an unauthenticated `fetch(...).../rest/continue-code/apply/<code>` PUT with no Authorization header and asserts the resulting challenge is solved — the app's own test suite exercises and asserts the exact unauthenticated-apply behavior the finding flags. Challenge progress itself is also global in-memory state (data/datacache `challenges`), not a per-account resource, further indicating this is a shared-instance training mechanic rather than an authorization boundary that was meant to exist."
    reason: ""

  # TC-C96FA3FE — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any unauthenticated visitor can browse the directory listing at GET /encryptionkeys and download the server's RSA JWT public key (jwt.pub) and the premium-content AES key (premium.key) via GET /encryptionkeys/<file>, letting them decode/verify JWTs offline and unlock paywalled 'premium' content without paying or holding any account."
    evidence: "server.ts:277-278 wires the unauthenticated `serveIndex('encryptionkeys', ...)` listing and `serveKeyFiles()` file route with no `security.isAuthorized()` middleware, and the app's own test suite asserts exactly this as expected behavior: test/api/file-serving.test.ts:135-141 (`GET /encryptionkeys` → expects HTTP 200 + directory-listing HTML) and test/api/file-serving.test.ts:149-153 (`GET /encryptionkeys/premium.key` → expects HTTP 200), i.e. the tests assert the opposite of this finding's invariant. This is also a named, first-class, scored OWASP Juice Shop CTF challenge: data/static/challenges.yml:255-263 defines 'Directory Listing' (key: directoryListingChallenge, description 'Access a confidential document.', hint 'The file you are looking for is not protected in any way.'); data/static/challenges.yml:844-856 defines 'Premium Paywall' (key: premiumPaywallChallenge, category 'Cryptographic Issues', hint 'How much protection does a sturdy top-quality door lock add to your house if you put the key under the door mat? Or hide the key in the nearby plant pot? Or tape the key to the underside of the mailbox?' — a direct, deliberate hint pointing at this exact exposed premium.key); data/static/challenges.yml:445-458 defines 'Forged Signed JWT' (key: jwtForgedChallenge) whose own hint states 'Getting your hands on the public RSA key the application employs for its JWTs is mandatory for this challenge' — i.e. retrieving jwt.pub via this exact unauthenticated route is the documented, required first step of a named, scored challenge. This is not a generic 'intentionally vulnerable app' label — three separate named challenges are built specifically around discovering and using these exposed key files."
    reason: ""

  # TC-06653596 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any registered customer can permanently upgrade their account to deluxe membership (normally $49) with a single POST to /rest/deluxe-membership that omits/nulls the paymentMode field, granting them the deluxeToken, deluxe pricing, and all deluxe-exclusive product access with zero charge to any payment instrument."
    evidence: "routes/deluxe.ts:24-40 only deducts/validates payment when req.body.paymentMode === 'wallet' or === 'card', otherwise falling through to grant the upgrade; routes/deluxe.ts:44-46 explicitly calls `challengeUtils.solveIf(challenges.freeDeluxeChallenge, () => security.verify(utils.jwtFrom(req)) && req.body.paymentMode !== 'wallet' && req.body.paymentMode !== 'card')` — i.e. the app's own challenge-solved detection checks for exactly this bypass condition as the winning criterion. data/static/challenges.yml:1349-1359 defines this exact behavior as the named, scored challenge 'Deluxe Fraud' (category 'Improper Input Validation', description 'Obtain a Deluxe Membership without paying for it.', with hints pointing directly at manipulating the payment-method parameter). test/cypress/e2e/deluxe.spec.ts:17-35 (describe block `challenge \"freeDeluxe\"`) logs in as a real user, POSTs to /rest/deluxe-membership with no paymentMode set, asserts the response status is 'success', and asserts `cy.expectChallengeSolved({ challenge: 'Deluxe Fraud' })` — the app's own test suite asserts the opposite of the finding's invariant. This is a named, first-class, scored CTF challenge deliberately shipped by the intentionally-vulnerable training app, not an accidental oversight."
    reason: ""

  # TC-DE8951C0 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any logged-in customer (not just an admin) can permanently add arbitrary products to the store catalog — setting name, description, price, and image — via POST /api/Products. Because the same endpoint accepts unsanitized HTML in fields like description, this is also the built-in mechanism for injecting a persisted XSS payload without ever touching the frontend UI."
    evidence: "server.ts:368-370 — the comment '/* Products: Only GET is allowed in order to view products */' sits directly above `app.post('/api/Products', security.isAuthorized())`, both explicitly tagged `vuln-code-snippet neutral-line changeProductChallenge`, and the next line (`app.put('/api/Products/:id', ...)`) is commented out and tagged `vuln-line changeProductChallenge` — part of the app's own tutorial vuln-code-snippet system (see also TC-4B096581, which covers the PUT half of this same annotated block). `security.isAuthorized()` (lib/insecurity.ts:52) only validates that the JWT is well-formed/signed (via expressJwt) and does not check `role`, so any authenticated customer passes it — unlike the role-checking `isAccounting()` (lib/insecurity.ts:154) that the finding's own remediation suggests modeling an `isAdmin()` after. data/static/challenges.yml:13-25 defines the named, scored challenge 'API-only XSS' (key: restfulXssChallenge, category 'XSS', description 'Perform a persisted XSS attack ... without using the frontend application at all', hints: 'You need to work with the server-side API directly. Try different HTTP verbs on different entities exposed through the API.' and 'Careless developers might have exposed API methods that the client does not even need.') — this is precisely the POST /api/Products-without-admin-check gap. test/cypress/e2e/restApi.spec.ts:6-42, under `describe('challenge \"restfulXss\"')`, contains a test titled 'should be possible to create a new product when logged in' that performs exactly this: a fetch POST to /api/Products using the logged-in user's Bearer token, then asserts `cy.expectChallengeSolved({ challenge: 'API-only XSS' })`. Reproduced live: logging in as jim@juice-sh.op (role customer) and POSTing to /api/Products returned HTTP 201 with a newly created product (id 1002), confirming the finding's observed behavior — but the create-without-admin-check gap is the app's own deliberate, first-class, scored challenge mechanism, not an accidental oversight."
    reason: ""

  # TC-1BBB1FAD — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any anonymous visitor (no account, no token) can PUT /api/Products/:id to change any product's price, name, or description in the live catalog — e.g. slashing prices to near-zero or inflating them, or defacing listings — because the isAuthorized() guard on that route is commented out. (Same root-cause route as the TC-4B096581 entry above; this entry covers the broader price/name/catalog-integrity consequence rather than the stored-XSS consequence.)"
    evidence: "server.ts:350 and :455 bracket this exact block with `// vuln-code-snippet start/end changeProductChallenge`; server.ts:370 marks the disabled guard itself `// app.put('/api/Products/:id', security.isAuthorized()) // vuln-code-snippet vuln-line changeProductChallenge`. data/static/challenges.yml defines this as the named, scored 'Product Tampering' challenge (key: changeProductChallenge, category 'Broken Access Control'), whose hint explicitly reads 'Theoretically there are three possible ways to beat this challenge: a) broken admin functionality, b) holes in RESTful API or c) possibility for SQL Injection' — i.e. exploiting exactly this unauthenticated REST write is one of the app's documented, intended solution paths. routes/verify.ts:206-221 (changeProductChallenge()) implements the server-side detector that marks the challenge solved once a product's description is tampered with via this route, confirming the app actively tracks and rewards this exact exploit."
    reason: ""

  # TC-EBE4A1B0 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any unauthenticated party can forge a JWT with alg:none and an empty signature, impersonating any user (including role: admin), and every isAuthorized()-protected endpoint accepts it (e.g. GET /rest/user/authentication-details returns the full user list), because verify() calls raw jws.verify (lib/insecurity.ts:55), which trusts the token's own alg header instead of pinning an allowed algorithm, and isAuthorized() calls expressJwt with only a secret and no algorithms restriction (lib/insecurity.ts:52)."
    evidence: "lib/insecurity.ts:52,55 — the exact unsigned-JWT bypass path; data/static/challenges.yml:1133-1144 defines the named, scored challenge 'Unsigned JWT' (key: jwtUnsignedChallenge, category 'Vulnerable Components', description 'Forge an essentially unsigned JWT token that impersonates the (non-existing) user jwtn3d@juice-sh.op', hints explicitly describing 'downgrading to no encryption at all' and to make sure the forged token is URL safe); routes/verify.ts:84-133 implements dedicated server-side detection — jwtChallenges() calls jwtChallenge(challenges.jwtUnsignedChallenge, req, 'none', /jwtn3d@/) on every request, which decodes the incoming token, checks hasAlgorithm(token,'none'), and calls challengeUtils.solveIf(...) — i.e. the app actively watches for and rewards exactly this exploit against any endpoint. This is a named, first-class, scored CTF challenge deliberately shipped by the intentionally-vulnerable training app, not an accidental oversight."
    reason: ""

  # TC-407256BC — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any unauthenticated user can craft a /redirect?to= URL that embeds an allowlisted URL as a substring (e.g. as a query-string value) to bypass the allowlist check and get redirected to an arbitrary attacker-controlled domain, enabling phishing/credential-harvesting pages under the trusted app's domain."
    evidence: "lib/insecurity.ts:121-140 — the block is explicitly wrapped in `// vuln-code-snippet start redirectCryptoCurrencyChallenge redirectChallenge` / `// vuln-code-snippet end ...` markers, with line 136 (`allowed = allowed || url.includes(allowedUrl)`) tagged `// vuln-code-snippet vuln-line redirectChallenge` — a substring check by design, not an oversight. routes/redirect.ts:16-18 shows the app's own challenge-solved detection: `challengeUtils.solveIf(challenges.redirectChallenge, () => isUnintendedRedirect(toUrl))`, where `isUnintendedRedirect` (routes/redirect.ts:26-31) uses the *correct* `utils.startsWith` prefix check — i.e. the app explicitly detects 'toUrl passed the loose substring allowlist check but would have failed a proper prefix check' as the winning condition for this challenge, and marks it solved when the exact bypass in this finding occurs. data/static/challenges.yml:1236-1248 defines this exact behavior as the named, scored challenge 'Allowlist Bypass' (category 'Unvalidated Redirects', tag 'Prerequisite', description 'Enforce a redirect to a page you are not supposed to redirect to', hints pointing at 'beat the allowlist of allowed redirect URLs' and 'tampering with the redirect mechanism', mitigationUrl to the Unvalidated Redirects and Forwards Cheat Sheet). This is a named, first-class, scored CTF challenge deliberately shipped by the intentionally-vulnerable training app — it ships two parallel implementations (a loose substring check that is exploitable, and a correct prefix-check helper used only to detect the exploit) purpose-built to teach and reward exactly this bypass, not an accidental oversight."
    reason: ""

  # TC-55B72995 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any unauthenticated user who uploads a crafted XML file to the deprecated B2B complaints file-upload endpoint (POST /file-upload) can read arbitrary files from the server's filesystem (e.g. /etc/passwd, /etc/shadow, application secrets, private keys, source code) via an XXE external entity, with the file contents returned directly in the HTTP 410 error response body. A related payload can also hang the parser for a Denial-of-Service."
    evidence: "lib/xml.ts:9-11 — comment 'Grants the WASM sandbox host filesystem access so external entities like file:///etc/passwd resolve - required for the XXE challenges' directly above the call to xmlRegisterFsInputProviders(); lib/xml.ts:32-34 — comment 'Parses XML with entity substitution and external entity loading enabled (intentionally vulnerable to XXE for the related challenges)' directly above parseXmlString, which explicitly ORs in XML_PARSE_NOENT | XML_PARSE_DTDLOAD to enable entity expansion and external DTD loading. routes/fileUpload.ts:76-90 calls parseXmlString on the uploaded file and then calls challengeUtils.solveIf(challenges.xxeFileDisclosureChallenge, ...) when the parsed output contains /etc/passwd or system.ini contents, and separately solves challenges.xxeDosChallenge when parsing times out. data/static/challenges.yml:1250-1281 defines both as named, scored, first-class challenges — 'XXE Data Access' (key xxeFileDisclosureChallenge, category 'XXE', hint 'The leverage point for this challenge is the deprecated B2B interface') and 'XXE DoS' (key xxeDosChallenge) — matching this exact endpoint, payload, and observed /etc/passwd disclosure. OWASP Juice Shop is an intentionally-vulnerable training/CTF application, and this specific XXE behavior is one of its named, documented, scored challenges, not an accidental oversight."
    reason: ""

  # TC-4EF35798 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any logged-in customer can overwrite every product review across all products and all user accounts in a single authenticated request, by sending a MongoDB query operator (e.g. {\"id\":{\"$gt\":\"\"}}) as the review id to PATCH /rest/products/reviews instead of a plain string id, permanently destroying all review content written by other users."
    evidence: "routes/updateProductReviews.ts:16-20 — the vulnerable code is explicitly wrapped in `vuln-code-snippet start/end noSqlReviewsChallenge forgedReviewChallenge` markers: `{ _id: req.body.id }` (line 18) is tagged `vuln-line noSqlReviewsChallenge forgedReviewChallenge` and `{ multi: true }` (line 20) is tagged `vuln-line noSqlReviewsChallenge`, i.e. the raw, untyped id and the multi-document update are deliberately unguarded. Line 23 immediately calls `challengeUtils.solveIf(challenges.noSqlReviewsChallenge, () => result.modified > 1)` — the app's own challenge-solved detection explicitly requires more than one review to be modified in a single call, which is precisely the finding's observed behavior (34 reviews overwritten at once). data/static/challenges.yml:793-802 defines the named, scored challenge 'NoSQL Manipulation' (key: noSqlReviewsChallenge, category 'Injection', description 'Update multiple product reviews at the same time.', hints pointing at MongoDB Query Operators and the UPDATE-statement equivalent). A related challenge on the same endpoint, 'Forged Review' (key: forgedReviewChallenge, data/static/challenges.yml:434-444, category 'Broken Access Control', description \"Post a product review as another user or edit any user's existing review.\"), covers the adjacent missing-ownership-check the finding's remediation guidance also flags. This is a named, first-class, scored CTF challenge deliberately shipped by the intentionally-vulnerable training app, not an accidental oversight."
    reason: ""

  # TC-9C7EE50A — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any authenticated user can read the first ~100 bytes of any file readable by the Node.js process within the application directory (package.json, config/default.yml, route source, view templates, etc.) by controlling which Handlebars layout file is rendered via POST /dataerasure's `layout` field — one file per request, an incremental local-file-read primitive that can leak config and source code."
    evidence: "routes/dataErasure.ts:103-118 — the POST handler resolves `req.body.layout` with `path.resolve()` and spreads `req.body` directly into `res.render('dataErasureResult', { ...req.body, ...themeVars })`, gated only by a three-string blocklist (`ftp`, `ctf.key`, `encryptionkeys`) that traversal outside those trivially bypasses; on success it calls `challengeUtils.solveIf(challenges.lfrChallenge, () => { return true })` — i.e. successfully rendering an attacker-chosen file as the layout is the literal win-condition for a named challenge, not an oversight. data/static/challenges.yml:1439-1456 defines this exact behavior as the named, scored challenge 'Local File Read' (key: lfrChallenge, category 'Vulnerable Components', tag 'Danger Zone', difficulty 5, description 'Gain read access to an arbitrary local file on the web server.', hints pointing solvers at LFR vulnerabilities in NodeJS template engines and at 'an easily forgettable endpoint'). The project's own Cypress e2e suite (test/cypress/e2e/dataErasure.spec.ts:6-28, describe block 'challenge \"lfr\"') POSTs the identical payload used in this finding — `layout=../package.json` — to /dataerasure and asserts `cy.expectChallengeSolved({ challenge: 'Local File Read' })`. The app's own tests assert the opposite of the reported invariant: this is a named, first-class, scored CTF challenge deliberately shipped by the intentionally-vulnerable training app, not an accidental oversight."
    reason: ""

  # TC-16957EC9 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any unauthenticated visitor can send GET /rest/track-order/test'%20||%20true%20||%20' and dump every order in the database — product names, quantities, prices, delivery status, ETAs, and partially-masked customer emails across every account — in a single request, by injecting a tautology into the MongoDB $where clause used to look up a single order."
    evidence: "routes/trackOrder.ts:15 skips id sanitization (only strips non-word/dash chars when challenges.reflectedXssChallenge is disabled; it is enabled by default on this non-Docker/Heroku/Windows host, per lib/utils.ts:151-183 getChallengeEnablementStatus), routes/trackOrder.ts:18 interpolates the unsanitized id directly into `$where: this.orderId === '${id}'`, and routes/trackOrder.ts:20 calls challengeUtils.solveIf(challenges.noSqlOrdersChallenge, () => result.data.length > 1) — the exploit condition (the query returning more than one order) is the app's own scoring hook for this challenge. data/static/challenges.yml:776-788 defines this exact behavior as the 'NoSQL Exfiltration' challenge (key: noSqlOrdersChallenge, category 'Injection', tag 'Danger Zone', difficulty 5, description 'All your orders are belong to us! Even the ones which don't.', hints pointing directly at the $where operator and the track-order endpoint). Live-reproduced against the running target: GET /rest/track-order/test'%20||%20true%20||%20' returned HTTP 200 with 11 orders spanning multiple accounts (admin, demo, jim), matching the finding's PoC exactly. This is one of Juice Shop's named, first-class, scored CTF challenges — not an accidental oversight or a generic 'intentionally vulnerable app' label."
    reason: ""

  # TC-DF943368 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any anonymous, unauthenticated caller can GET /rest/admin/application-configuration and receive the full server configuration — the Google OAuth client ID and its 13 authorized redirect URIs, all challenge trigger payloads/URLs (e.g. the XSS bonus payload, the CSRF and product-tampering challenge target URLs), all 56 product records, and server settings like port/baseUrl — with no credential, and the response is unchanged even when an invalid Bearer token is supplied."
    evidence: "routes/appConfiguration.ts:9-16 — the handler `retrieveAppConfiguration()` deliberately redacts exactly one field before responding (`delete safeConfig.application.chatBot.llmApiUrl`), which shows the author consciously reviewed what this publicly-served response exposes and chose to strip only that one secret, leaving everything else (including the OAuth client ID and challenge data) intentionally left in the public response; server.ts:607 registers the route with no auth middleware, consistent with that design. The project's own test suite asserts the opposite of this finding's invariant: test/api/administration.test.ts:31-41 (`describe('/rest/admin/application-configuration')`) calls the endpoint with no authentication and asserts `res.status === 200` plus a populated `config` body — i.e. unauthenticated 200 is the suite's documented expected behavior, not an accidental gap. The frontend also relies on unauthenticated access by design: frontend/src/app/Services/configuration.service.ts:121 fetches this endpoint on app bootstrap before any login, and frontend/src/hacking-instructor/helpers/helpers.ts:42/149 fetch it unauthenticated from client-side code to check challenge progress. Already-accepted TC-2BEBBAC0 in this same file independently reproduces this exact endpoint being readable cross-origin via the app's wildcard CORS policy, corroborating that its public exposure is a known, standing characteristic of this app rather than a fresh discovery. A Google OAuth *client* ID is also not a secret per the OAuth spec (it is embedded in every client-side authorization redirect), further narrowing the residual risk here to the challenge/product data, which the app already serves through numerous other unauthenticated endpoints (e.g. GET /rest/products/search)."
    reason: ""

  # TC-3B6A3D78 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any authenticated user (customer, accounting, or deluxe — admin not required) can call GET /rest/user/authentication-details and retrieve the complete 27-account user roster (emails, roles, lastLoginIp, isActive, and 6 plaintext deluxeTokens), letting an attacker impersonate any deluxe-tier account on deluxe-gated endpoints without knowing its credentials."
    evidence: "server.ts:398 gates the route with only `security.isAuthorized()` — there is no `isAdmin()` middleware anywhere in the codebase (grep of lib/insecurity.ts, server.ts, routes/*.ts finds none; only `isAccounting()` and `isAuthorized()` exist), so no backend route ever enforces an admin-only check. This endpoint backs the Angular /administration page (frontend/src/app/administration/administration.component.ts calls userService.find()), whose route is guarded only client-side by AdminGuard (frontend/src/app/app.routing.ts:78-82, wrapped in `vuln-code-snippet ... adminSectionChallenge` markers, with `path: 'administration'` explicitly tagged `vuln-line adminSectionChallenge`). data/static/challenges.yml:53-68 defines the named, scored challenge 'Admin Section' (key: adminSectionChallenge, category 'Broken Access Control', description 'Access the administration section of the store.', hint 'There is some access control in place, but there are at least three ways to bypass it.'), solved server-side by routes/verify.ts:68 when the page's 19px.png asset is requested. Reaching this data (via the page or directly) as a non-admin authenticated user is precisely the documented objective of this first-class CTF challenge, not an accidental oversight; the routes/authenticatedUsers.ts handler even masks `password` and `totpSecret` with asterisks but deliberately leaves other fields — including `deluxeToken` — unmasked."
    reason: ""

  # TC-67B5FD2E — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any authenticated user, regardless of role (customer, deluxe, accounting, or even a token carrying no role claim at all), can call GET /api/Users to list every registered account (id, email, role, lastLoginIp, deluxeToken, profileImage, isActive, timestamps) and GET /api/Users/:id to fetch any single account's record by id, including admin accounts — giving any logged-in user a full directory of the platform's users and their privilege levels."
    evidence: "server.ts:361-365 wires `app.get('/api/Users', security.isAuthorized())` and `app.route('/api/Users/:id').get(security.isAuthorized())` with no role check beyond 'has a valid JWT' (contrast with the role-gated `security.isAccounting()` used elsewhere, e.g. on /rest/order-history/orders). The project's own API test suite asserts this exact behavior as correct, not as a bug: test/api/user.test.ts:27-44 — the 'GET all users' test authenticates with `security.authorize()` called with no user/role argument (a valid, authenticated token that carries no role claim) and asserts `res.status === 200` (not 403), and the immediately following test 'GET all users doesnt include passwords' asserts only that the `password` field is absent from each returned record, implying every other field (email, role, lastLoginIp, deluxeToken, etc.) is expected to be present in the response to a non-privileged caller. test/api/user.test.ts:198-220 ('GET existing user by id') likewise asserts GET /api/Users/:id returns 200 for the same non-role-scoped authenticated header, only rejecting PUT/DELETE (401). Live reproduction against the running target (jim@juice-sh.op, a plain customer-role account, per credentials CR_001) confirms current behavior matches the shipped tests: unauthenticated GET /api/Users returns 401; with jim's token GET /api/Users returns 200 with all 42 accounts (10 admin, 8 deluxe, 23 customer, 1 accounting) including email/role/deluxeToken/lastLoginIp fields, and GET /api/Users/1 returns the full admin@juice-sh.op record. Because the app's own test suite asserts the opposite of the finding's invariant (that a non-admin caller should be rejected), this is documented, tested, pre-existing behavior rather than an unnoticed regression."
    reason: ""

  # TC-2BBDCAA9 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any unauthenticated visitor can upload a zip file to POST /file-upload whose entry names contain `../../` path traversal, causing the server to write the entry's content to arbitrary file paths outside uploads/complaints/ that the app process can reach (e.g. overwriting ftp/legal.md), because the boundary check uses `includes(path.resolve('.'))` — which is always true — instead of a startsWith prefix check, and the write itself uses the raw unvalidated relative path rather than the resolved one."
    evidence: "routes/fileUpload.ts:32 — immediately next to the vulnerable guard, the code calls `challengeUtils.solveIf(challenges.fileWriteChallenge, () => { return absolutePath === path.resolve('ftp/legal.md') })`, i.e. the app's own challenge-solved detection is wired to fire exactly when this precise path-traversal write lands on ftp/legal.md. data/static/challenges.yml:70-86 defines this exact behavior as the named, scored challenge 'Arbitrary File Write' (key: fileWriteChallenge, category 'Vulnerable Components', tags 'Danger Zone'/'Prerequisite', difficulty 6, description 'Overwrite the <a href=\"/ftp/legal.md\">Legal Information</a> file.', hints pointing at a third-party library vulnerability behind a file-upload feature, mitigationUrl to the OWASP Vulnerable Dependency Management cheat sheet). The challenge is gated by `utils.isChallengeEnabled(challenges.fileWriteChallenge)` in routes/fileUpload.ts:44 and is `disabledEnv: [Docker, Heroku]` per data/static/challenges.yml:84-86, but niro/harness/start.sh starts the app via `node build/app.js` directly (not the Docker image), so the challenge remains enabled — consistent with the finding's observed reproduction. This is not a generic 'intentionally vulnerable app' label — it is a specific, named, first-class CTF challenge whose win-condition is this precise zip-slip arbitrary-file-write exploit."
    reason: ""

  # TC-1CBE21C1 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any unauthenticated caller can exfiltrate every user's email address and password hash from the database in a single HTTP request to GET /rest/products/search?q=, by closing the Products WHERE clause and appending a UNION SELECT against the Users table (e.g. q='))UNION SELECT email,password,1,1,1,1,1,1,1 FROM Users--), enabling offline password cracking and account takeover across the entire user base."
    evidence: "routes/search.ts:18-23 — the vulnerable query is wrapped in explicit `// vuln-code-snippet start/end unionSqlInjectionChallenge dbSchemaChallenge` markers, with the offending template-literal line tagged `// vuln-code-snippet vuln-line unionSqlInjectionChallenge dbSchemaChallenge`; lines 25-38 immediately run dedicated challenge-solved detection (`challengeUtils.notSolved(challenges.unionSqlInjectionChallenge)` -> fetches all users and checks `dataString` contains every user's email and password, then calls `challengeUtils.solve(challenges.unionSqlInjectionChallenge)`), i.e. the app's own code explicitly recognizes and scores this exact email+password exfiltration. data/static/challenges.yml:1167-1178 defines the named, scored challenge 'User Credentials' (key: unionSqlInjectionChallenge, category 'Injection', description 'Retrieve a list of all user credentials via SQL Injection.', difficulty 4, hints that explicitly walk the player through crafting 'a corresponding UNION SELECT attack' against the same endpoint). The app's own Cypress suite performs this identical attack and asserts it as a win condition: test/cypress/e2e/search.spec.ts:32-39, describe block `challenge \"unionSqlInjection\"`, sends `q=')) union select id,'2','3',email,password,'6','7','8','9' from users--` to /rest/products/search and asserts `cy.expectChallengeSolved({ challenge: 'User Credentials' })` — the app's own tests assert the opposite of the finding's invariant. This is a named, first-class, scored CTF challenge deliberately shipped by the intentionally-vulnerable training app (not a generic 'demo app' label) — the app deliberately ships this exact exploitable behavior, with dedicated tracking code, for users to find and solve."
    reason: ""

  # TC-0A9B1741 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any unauthenticated visitor can browse a directory listing at GET /support/logs and download individual server log files via GET /support/logs/<file>, including the daily access.log (every request's source IP, full URL path — e.g. admin/auth endpoint paths hit by other clients, HTTP status, response size, and timestamp) and audit.json (which additionally leaks the server's absolute filesystem path). This hands an unauthenticated attacker a full traffic/API map to plan follow-on attacks, with no login or token required."
    evidence: "server.ts:267-290 wraps this exact block in `// vuln-code-snippet start directoryListingChallenge accessLogDisclosureChallenge` ... `// vuln-code-snippet end ...` markers, with server.ts:281 (`app.use('/support/logs', serveIndexMiddleware, serveIndex('logs', ...))`) and server.ts:283 (`app.use('/support/logs/:file', serveLogFiles())`) individually tagged `// vuln-code-snippet vuln-line accessLogDisclosureChallenge`. data/static/challenges.yml:30-40 defines this as the named, scored challenge 'Access Log' (key: accessLogDisclosureChallenge, category 'Observability Failures', difficulty 4, description 'Gain access to any access log file of the server.', mitigationUrl to the OWASP Logging Cheat Sheet). routes/verify.ts:63-73 (`accessControlChallenges()`) explicitly pattern-matches the exact URL shape being exploited (`url.match(/access\\.log(0-9-)*/)`) and calls `challengeUtils.solveIf(challenges.accessLogDisclosureChallenge, ...)` to mark it solved when hit — confirming the app's authors deliberately built this endpoint/response to be found and exploited as a first-class, named CTF challenge, not a generic 'intentionally vulnerable app' label."
    reason: ""

  # TC-53D9190B — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any logged-in customer can permanently delete any other customer's feedback record (e.g. DELETE /api/Feedbacks/13 with their own valid JWT, no ownership of the record required), letting an attacker silently erase other users' reviews and manipulate the store's public reputation data."
    evidence: "server.ts:361 guards `/api/Feedbacks/:id` with only `security.isAuthorized()` (any valid JWT passes) before finalé's generic Sequelize CRUD handles the DELETE — no ownership/role comparison against `feedback.UserId` exists anywhere on this route. routes/verify.ts:225-233 `feedbackChallenge()` — invoked from `databaseRelatedChallenges()` on every request (routes/verify.ts:170-172) — counts rows with `rating: 5` and calls `challengeUtils.solve(challenges.feedbackChallenge)` once that count reaches zero, i.e. the app's own challenge-solved detection is wired to fire precisely when all 5-star feedback (necessarily including other users' records, since a normal customer has at most one own review) has been deleted. data/static/challenges.yml:393-402 defines this exact behavior as the named, scored challenge 'Five-Star Feedback' (key: feedbackChallenge, category 'Broken Access Control', difficulty 2, description 'Get rid of all 5-star customer feedback.', hints pointing at the admin section and a JavaScript-console error that appears when deletion is attempted from the UI, mitigationUrl to the OWASP Authorization Cheat Sheet). This is a named, first-class, scored CTF challenge deliberately shipped by the intentionally-vulnerable training app — not a generic 'demo app' label — with the missing ownership check being the specific, documented objective players are meant to exploit."
    reason: ""

  # TC-0F4305A0 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any authenticated customer can publish a product review under any other user's email address (including admins) via PUT /rest/products/:id/reviews, causing false statements to appear permanently attributed to victims and visible to all shoppers; the same forgery is also possible on the edit path (PATCH /rest/products/reviews), letting an attacker overwrite another user's existing review text while it stays attributed to that victim."
    evidence: "routes/createProductReviews.ts:23-29 inserts `author: req.body.author` verbatim (the server-resolved `user` from `security.authenticatedUsers.from(req)` at line 16 is never used for the author field), and line 17-20 calls `challengeUtils.solveIf(challenges.forgedReviewChallenge, () => user?.data?.email !== req.body.author)` u2014 i.e. the app's own challenge-solved detection fires precisely when the caller supplies an `author` that does not match their own identity. routes/updateProductReviews.ts additionally wraps the analogous edit-path bug in explicit `// vuln-code-snippet start/end noSqlReviewsChallenge forgedReviewChallenge` markers with `vuln-line forgedReviewChallenge` tags (lines 13, 16), and line 24 calls the same `challengeUtils.solveIf(challenges.forgedReviewChallenge, ...)` check against `result.original[0].author !== user.data.email`. data/static/challenges.yml:434-444 defines the named, scored challenge 'Forged Review' (category 'Broken Access Control', description \"Post a product review as another user or edit any user's existing review.\", difficulty 3, hints pointing directly at intercepting the review-submission request, mitigationUrl to the OWASP Authorization Cheat Sheet). The app's own Cypress suite (test/cypress/e2e/noSql.spec.ts:74-116, describe block `challenge \"Forged Review\"`) performs this exact PATCH-as-another-user exploit and asserts `cy.expectChallengeSolved({ challenge: 'Forged Review' })` u2014 the app's own test suite asserts the opposite of the finding's invariant. This is a named, first-class, scored CTF challenge deliberately shipped by the intentionally-vulnerable training app, not an accidental oversight u2014 same pattern already accepted for TC-83CBE6E9 (Change Bender's Password) and TC-7FF95ADE (Manipulate Basket)."
    reason: ""

  # TC-76D66C43 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any anonymous visitor, without an account or Authorization header, can POST to /snippets/verdict and /snippets/fixes to mark a coding challenge's find-it/fix-it phases as solved, flipping the shared, instance-wide `codingChallengeStatus` column (not per-user) for that challenge — e.g. adminSectionChallenge or resetPasswordBjoernOwaspChallenge going from 0 to 2 with no login."
    evidence: "server.ts:671,673 register `POST /snippets/verdict` (checkVulnLines) and `POST /snippets/fixes` (checkCorrectFix) with no `security.isAuthorized()` middleware, unlike state-changing routes such as `POST /api/Products` (server.ts:369). This is exercised, unauthenticated, by the project's own first-party test suite: test/api/vuln-code-snippet.test.ts (describe 'snippets/verdict') and test/api/vuln-code-fixes.test.ts (describe '/snippets/fixes') call both POST routes with plain `request(app).post(...)` — no Authorization header, no login step anywhere in either file — and assert HTTP 200 with `verdict: true`/`codingChallengeStatus` transitions on success, i.e. the app's own tests assert the opposite of the finding's invariant (401-when-unauthenticated). Frontend side, the `/coding-challenge/:challengeKey` route (frontend/src/app/app.routing.ts:264-267) has no `canActivate` guard (contrast with LoginGuard-protected routes at lines 95,100,105,110,119), consistent with the find-it/fix-it coding-challenge UI being reachable without logging in. Separately, models/challenge.ts:147-148 defines `solved`/`codingChallengeStatus` as plain columns on the single `Challenges` table with no per-user foreign key — OWASP Juice Shop's entire challenge/progress model is a single, global, instance-wide scoreboard by design (every challenge solve, coding-challenge or otherwise, is global to the running instance, not scoped per visitor), so the 'wipes progress for all legitimate users' consequence describes how every challenge in this app already works, not a defect specific to these two routes. Live-reproduced against the running target: `POST /snippets/verdict` with body `{\"key\":\"loginJimChallenge\",\"selectedLines\":[2]}` and no Authorization header returned HTTP 200 `{\"verdict\":false}`."
    reason: ""

  # TC-A2475B23 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any authenticated customer can check out any other customer's basket by supplying a different basket ID to POST /rest/basket/:id/checkout — this permanently empties the victim's basket (every BasketItem for that basket is deleted and, if a coupon was applied, it is cleared) and generates a real order confirmation recorded under the attacker's own identity, with no comparison anywhere in the handler between the authenticated caller and the basket's owning UserId."
    evidence: "routes/order.ts placeOrder() (lines 32-188) fetches the basket by req.params.id alone and never checks it against security.authenticatedUsers.from(req) before deleting BasketItems and recording the order — no ownership guard exists in the handler at all. The app's own committed test suite already exercises and asserts exactly this cross-user path as correct, passing behavior: test/api/basket.test.ts:131-135 logs in once as jim (UserId 2, own basket id 2, per data/datacreator.ts:482) in the top-level `before` block, then does `POST /rest/basket/1/checkout` with jim's auth header — basket 1 belongs to UserId 1/admin (data/datacreator.ts:481, seeded with items at :502-514) — and asserts `res.status === 200` with a defined `orderConfirmation`. test/api/basket.test.ts:143-153 repeats the same cross-user pattern: it adds an item to jim's own basket (BasketId 2) then checks out `POST /rest/basket/3/checkout` — basket 3 belongs to UserId 3/bender (datacreator.ts:483, seeded at :522-525) — still using jim's auth header, again asserting HTTP 200 with an orderConfirmation. Both tests assert the literal opposite of this finding's invariant (that checkout should fail/403 when the basket doesn't belong to the caller) using the exact same mechanism (checkout of a basket ID the authenticated user does not own) the finding demonstrates against jim/bender/admin. The related GET /rest/basket/:id endpoint's lack of an ownership check is separately a named, first-class, scored Juice Shop challenge ('View Basket' / basketAccessChallenge, data/static/challenges.yml:1197-1210, routes/basket.ts:21-24), reinforcing that basket-ID-based access across users is an established, deliberately loose pattern in this codebase rather than an isolated oversight in the checkout path alone."
    reason: ""

  # TC-73BD0FFD — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any caller — authenticated or not — can read any other user's recycle records (quantity, address id, pickup flag, date) via GET /api/Recycles/:id, either one at a time by guessing a numeric id, or many at once by supplying a JSON array (e.g. GET /api/Recycles/%5B1%2C2%2C3%5D), because the route has no auth middleware and the handler applies no ownership filter to the Sequelize query."
    evidence: "server.ts:388 registers `app.get('/api/Recycles/:id', recycles.getRecycleItem())` with no `security.isAuthorized()` guard, unlike the adjacent POST (server.ts:386, `security.isAuthorized()`) and PUT/DELETE (server.ts:389-390, `security.denyAll()`) on the same resource — a deliberate per-verb policy, not an oversight. routes/recycles.ts:11-21 confirms the handler does `RecycleModel.findAll({ where: { id: JSON.parse(req.params.id) } })` with no `UserId` clause at all, for any id shape. The project's own API test suite asserts the opposite of this finding's invariant: test/api/recycle.test.ts:47-75 ('Will GET existing recycle from this endpoint') calls `request(app).get('/api/Recycles/1')` with **no Authorization header** and asserts `res.status === 200`, then iterates every returned item checking only field types/shapes (id, UserId, AddressId, quantity, isPickup, date) — never asserting that the returned `UserId` matches any particular caller, i.e. the suite's own passing expectation is 'anonymous GET succeeds and ownership is not checked.' Live reproduction against the running target confirms current behavior matches the shipped test exactly: GET /api/Recycles/1 with no Authorization header returns 200 with UserId:2's record; GET /api/Recycles/%5B1%2C2%2C3%5D with no Authorization header, and again with jim's (CR_001) bearer token, both return the identical three records spanning UserId 2, 3, and 4. No named/scored challenge in data/static/challenges.yml references Recycles, so this is not a marked CTF challenge — but the missing-ownership-check behavior (independent of the array-id trick, which only batches the same pre-existing gap) is already codified as expected, passing behavior by the project's own test suite, not merely an unnoticed regression."
    reason: ""

  # TC-A2398F3C — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any caller — including one with no login at all, not just an ordinary authenticated user — can permanently overwrite the guidance text of any challenge hint via PUT /api/Hints/:id, replacing it with arbitrary content that every other player sees immediately, because the endpoint enforces no authentication and no restriction on which fields can be written."
    evidence: "server.ts:375-379 — the comment directly above the route reads 'Hints: GET and PUT hints allowed. Everything else forbidden', and the chain only adds `.get(security.denyAll())` and `.delete(security.denyAll())`, deliberately omitting `.put(...)` so PUT falls through to the generic Finale/Sequelize CRUD handler registered for the Hint model at server.ts:481-507, which applies no auth middleware and no field allowlist. This exact routing (open PUT, denied GET/DELETE) is preserved identically across all four teaching variants of the neighboring 'changeProductChallenge' codefix exercise, including the canonical answer key data/static/codefixes/changeProductChallenge_3_correct.ts:22-26 — i.e. when Juice Shop's own maintainers wrote what they define as the *correct, secure* version of this exact block of routes, they kept Hints PUT unrestricted, confirming it is deliberate rather than an oversight. The frontend confirms the intended use: frontend/src/app/score-board/score-board.component.ts:181-189 `unlockHint()` calls `hintService.put(hintId, { unlocked: true })` (frontend/src/app/Services/hint.service.ts:26-28) with no privilege check, so ordinary players are expected to write to this endpoint as a core gameplay mechanic; lib/challengeUtils.ts:37-38 treats a hint's `unlocked` flag as a single global attribute (not per-user), reinforcing that hint records are an intentionally shared, low-stakes convenience resource rather than security-sensitive data. Live reproduction against the running target: PUT /api/Hints/3 with a text body returned HTTP 200 both with a freshly-registered customer's Bearer token and with no Authorization header at all, confirming the absence of access control is total and matches the documented design rather than a partial gap. The finding's own PoC data (hints 1 and 2 already showing prior 'HACKED HINT' tampering) further corroborates this is a long-standing, freely-writable resource rather than a fresh regression; the probe text was restored to the original hint content after the repro."
    reason: ""

  # TC-48DE24E3 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any unauthenticated visitor can send GET /rest/products/1%20||%20true/reviews (or any product-id payload that turns the query into a tautology) and receive every review for every product in the database in a single response — 31+ review objects spanning 20+ products and 13 distinct reviewer emails — because the id parameter is concatenated unsanitized into a MongoDB `$where` clause (`this.product == ' + id`) that evaluates arbitrary JavaScript inside the database engine."
    evidence: "routes/showProductReviews.ts:30-31 — the comment 'Truncate id to avoid unintentional RCE' sits directly above `const id = !utils.isChallengeEnabled(challenges.noSqlCommandChallenge) ? Number(req.params.id) : utils.trunc(req.params.id, 40)`, i.e. the id is only coerced to a safe Number when the named challenge is OFF; when it is ON (the case in this harness — non-Docker/non-Heroku host, `challenges.safetyMode: auto`, per lib/utils.ts:151-184 `getChallengeEnablementStatus`, already established for the sibling route by the already-accepted TC-16957EC9 entry above) the raw string is passed straight into the `$where: 'this.product == ' + id` query built at line 36. data/static/challenges.yml:759-774 defines this exact endpoint/mechanism as the named, scored challenge 'NoSQL DoS' (key: noSqlCommandChallenge, category 'Injection', tag 'Danger Zone', description 'Let the server sleep for some time.', mitigationUrl to the OWASP DoS Cheat Sheet), whose own solve hook at showProductReviews.ts:38 (`challengeUtils.solveIf(challenges.noSqlCommandChallenge, () => (t1 - t0) > 2000)`) rewards injecting a `sleep()` call through this same unsanitized id parameter — i.e. the app's own challenge system is built around this parameter accepting arbitrary injected JavaScript, of which a boolean tautology (`1 || true`) returning every document is a textbook, equally foreseeable consequence of the identical injection point, not a different bug. The project's own test suites confirm the parameter is meant to accept injected commands rather than reject them: test/api/product-review.test.ts:37-42 ('GET product reviews attack by injecting a mongoDB sleep command') sends `GET /rest/products/sleep(1)/reviews` and asserts HTTP 200 (success, not rejection); test/cypress/e2e/noSql.spec.ts:6-27 (describe 'challenge \"NoSQL DoS\"') drives the identical sleep-injection attack against this same route and asserts `cy.expectChallengeSolved({ challenge: 'NoSQL DoS' })`; the immediately adjacent test block (noSql.spec.ts:30-49, describe 'challenge \"NoSQL Exfiltration\"') demonstrates the exact same boolean-tautology technique (`' || true || '`) against the sibling `$where`-based endpoint (routes/trackOrder.ts, already accepted as TC-16957EC9) to exfiltrate all records — establishing that boolean-bypass mass-exfiltration via an unsanitized `$where` id parameter is the app's own documented, intended exploit pattern for this class of endpoint, not an accidental oversight specific to reviews."
    reason: ""

  # TC-19DD7D31 — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any unauthenticated visitor can retrieve the application's own annotated source-code snippets and their fix-candidate variants (GET /snippets/:challenge, GET /snippets/fixes/:key) with no login required, including a snippet that shows the literal SQL injection line used by the loginAdminChallenge, giving an attacker a map of documented vulnerable code before attempting any attack. (Same route family as the already-accepted TC-76D66C43 above, which covers the POST/write-verdict side of these routes; this entry covers the GET/read-disclosure side.)"
    evidence: "This is Juice Shop's first-class, scored 'Find It' / 'Fix It' coding-challenge mini-game, not an oversight. The snippets are generated from `vuln-code-snippet start/vuln-line/neutral-line/end` build-time annotations placed directly on the vulnerable source, e.g. routes/login.ts:17,34,56 tags the exact SQL-injection line with `vuln-code-snippet vuln-line loginAdminChallenge loginBenderChallenge loginJimChallenge` — the app's authors deliberately annotate their own vulnerable lines so this exact content can be extracted and served as a teaching snippet. server.ts:669-673 registers all four /snippets routes with no `security.isAuthorized()`, consistent with every other route in this same annotated block being reachable pre-login. The frontend serves these at route `coding-challenge/:challengeKey` (frontend/src/app/app.routing.ts:264-267) with no `canActivate` guard, unlike the ~6 other routes in the same file that explicitly require `LoginGuard` (e.g. lines 95,100,105,110,119) — the UI itself is built to let anyone, logged in or not, open the challenge and view the snippet/fixes. The app's own test suite asserts the opposite of this finding's invariant: test/api/vuln-code-snippet.test.ts:58-64 ('GET code snippet retrieval for challenge with code snippet') calls GET /snippets/loginAdminChallenge with no auth and asserts HTTP 200 with a string snippet body; test/api/vuln-code-fixes.test.ts exercises GET /snippets/fixes/:key the same unauthenticated way and asserts 200 with fix candidates. This is the identical intentional-design pattern already accepted for the sibling POST routes in TC-76D66C43."
    reason: ""

  # TC-CC7F62FD — Added by the tool — accepted, rationale not yet documented:
  - consequence: "Any authenticated customer can permanently overwrite the review text of any other user (including admin) via PATCH /rest/products/reviews, by supplying that user's review _id — author attribution is left unchanged while the message content is silently replaced. Live-reproduced against the running target with isolated, self-created test data: logged in as accountant@juice-sh.op, created a fresh review on product 5 (message 'NIRO-VERIFY-ACCOUNTANT-OWN-...', _id c3jNPaExgAtsnj5PG); then, authenticated only as jim@juice-sh.op (a different user, no relation to that review), sent PATCH /rest/products/reviews {id: c3jNPaExgAtsnj5PG, message: 'HIJACKED-BY-JIM-NIRO-VERIFY'} and received HTTP 200 {modified:1}; GET /rest/products/5/reviews confirmed the tampered message persisted. A positive control (accountant editing their own review) also returned 200 {modified:1}, confirming the environment is healthy and the gap is specifically the missing ownership check, not a broken harness."
    evidence: "routes/updateProductReviews.ts:14-30 filters the update solely by `{ _id: req.body.id }` (line 18) with `{ multi: true }` (line 20) — the `user` resolved at line 16 via `security.authenticatedUsers.from(req)` is read only for challenge-solved bookkeeping (line 24), never used to constrain the query — and the whole block is wrapped in explicit `// vuln-code-snippet start/end noSqlReviewsChallenge forgedReviewChallenge` markers with the vulnerable lines individually tagged `vuln-line forgedReviewChallenge`/`vuln-line noSqlReviewsChallenge`. Line 24 calls `challengeUtils.solveIf(challenges.forgedReviewChallenge, () => ... result.original[0].author !== user.data.email && result.modified === 1)` — i.e. the app's own challenge-solved detection is wired to fire precisely when an authenticated user overwrites a review it does not own, exactly this finding's scenario. data/static/challenges.yml:434-444 defines the named, scored challenge 'Forged Review' (category 'Broken Access Control', description \"Post a product review as another user or edit any user's existing review.\", difficulty 3, hints pointing directly at intercepting the review-submission/edit request, mitigationUrl to the OWASP Authorization Cheat Sheet). The app's own Cypress suite performs this exact attack and asserts it as the intended, winning outcome: test/cypress/e2e/noSql.spec.ts:74-116, describe block `challenge \"Forged Review\"`, test `'should be possible to edit any existing review'` — logs in as a non-owner user (mc.safesearch), fetches another user's existing review id via GET /rest/products/1/reviews, PATCHes it via /rest/products/reviews with an arbitrary message, and asserts `cy.expectChallengeSolved({ challenge: 'Forged Review' })` (line 114) — the app's own test suite asserts the literal opposite of this finding's invariant. This is a named, first-class, scored CTF challenge deliberately shipped by the intentionally-vulnerable training app, not an accidental oversight — the identical PATCH-path behavior (bundled together with the sibling PUT-path author-forgery bug) was already accepted for TC-0F4305A0; this entry covers the same code path surfaced again as a separate finding."
    reason: ""
