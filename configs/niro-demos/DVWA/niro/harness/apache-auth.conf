# Preserve bearer Authorization for the PHP API controllers. Apache otherwise
# consumes the header before PHP can expose it as HTTP_AUTHORIZATION.
SetEnvIf Authorization "(.+)" HTTP_AUTHORIZATION=$1
